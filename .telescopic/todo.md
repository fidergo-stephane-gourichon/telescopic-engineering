Telescopic action list
======================

- **navbar generator and linter**: write a tool that generates the full set of navigation bars from a properly structured set of files (the filesystem structuration pattern). The same tool should double as a linter: check structural consistency and report issues (missing indexes, broken links, prefix/order mismatches against the order of mention in the parent document, sub-documents carrying stage nav bars, and similar). Distribute it with the telescopic skill.
